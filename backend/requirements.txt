# requirements.txt (merged & cleaned – April 2026)
# Previously removed: ifcopenshell, ezdxf (no IFC/DXF needed)
# Removed this pass (dead code audit): pytesseract, paddleocr (OCR replaced by
# vision LLMs), pdf2image, PyPDF2, camelot-py (PyMuPDF covers all PDF work),
# open3d (trimesh + pygltflib suffice), sqlalchemy, alembic, psycopg2-binary
# (no ORM / Postgres; job_store uses sqlite3 stdlib), aiohttp (only httpx +
# requests are used).

# PyTorch CUDA builds are not on PyPI — must come from the PyTorch wheel index.
--extra-index-url https://download.pytorch.org/whl/cu128

# ----------------
# Web Framework
# ----------------
fastapi==0.128.7
uvicorn[standard]==0.24.0.post1   # minor patch bump if desired (safe)
python-multipart==0.0.6
websockets>=13.0.0,<15.1.0
python-jose[cryptography]==3.3.0

# ----------------
# AI & Machine Learning
# ----------------
ultralytics==8.4.9      # YOLOv8 for element detection
torch==2.10.0+cu128              # PyTorch (required by YOLO)
torchvision==0.25.0+cu128       # Computer vision utilities

# ----------------
# Computer Vision
# ----------------
opencv-python>=4.11.0.86   # Image processing
Pillow==10.1.0            # → Pillow (capitalized consistently)

# ----------------
# PDF Processing
# ----------------
PyMuPDF==1.23.7           # PDF to image / fitz

# ----------------
# 3D Processing & Geometry
# ----------------
numpy==1.26.2             # Numerical computations
trimesh==4.0.5            # Mesh operations (used by gltf_exporter)
scipy==1.11.4             # Scientific computing
pygltflib==1.16.1         # glTF export for web viewer

# ----------------
# HTTP & Networking
# ----------------
requests>=2.32.3          # Sync HTTP (Revit server, etc.); >=2.32.3 required for urllib3 2.x + chardet 7.x compat
httpx>=0.28.1             # Async HTTP client

# ----------------
# Data Validation
# ----------------
pydantic>=2.7.0
pydantic-settings>=2.1.0

# ----------------
# File Operations & Env
# ----------------
aiofiles==23.2.1          # Async file I/O
python-dotenv==1.0.0      # Environment variables

# ----------------
# Task Queue (Optional – comment out if unused)
# ----------------
# celery==5.3.4
# redis==5.0.1

# ----------------
# Logging & Utilities
# ----------------
loguru==0.7.2             # Better logging
email-validator==2.3.0    # Email validation
psutil==5.9.6             # System resource monitoring (DoS prevention)
async-timeout==4.0.3      # Async timeouts (Security layer)

# ----------------
# Testing
# ----------------
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0

# ----------------
# Qwen2.5
# ----------------
transformers>=4.45.0
accelerate==1.12.0
qwen-vl-utils==0.0.14

# ----------------
# Semantic Analyzer & Chat
# ----------------
anthropic==0.79.0         # Claude API (semantic analysis + optional chat)
google-genai==1.62.0      # Gemini API (semantic analysis + optional chat)

# ----------------
# MCP (Model Context Protocol) — P3 Revit BIM server
# ----------------
mcp>=1.0.0                # MCP Python SDK (Anthropic) — for backend/mcp/server.py

# GPU Support:
# -----------
# For faster YOLO inference with GPU:
# - Install CUDA toolkit
# - Install: torch with CUDA support
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
#
# Production Additions:
# --------------------
# Consider adding:
# - gunicorn (production WSGI server)
# - sentry-sdk (error monitoring)
# - prometheus-client (metrics)
# - python-jose (JWT auth)

bitsandbytes==0.49.2

# ----------------
# Grid Detector utilities
# ----------------
omegaconf==2.3.0
scikit-image==0.25.2
